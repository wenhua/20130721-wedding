<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="assets/bootstrap/css/bootstrap.min.css" rel="stylesheet">
  <link href="assets/bootstrap/css/bootstrap-responsive.min.css"  rel="stylesheet" media="all" />
  <link href="css/blog.css" rel="stylesheet">

  <title><%= title %></title>
  <link rel="shortcut icon" type="image/png" href="images/favicon.png" />
</head>

<body>

  <!-- ==== Navbar ================================================== -->
  <div class="container">

      <div class="masthead">
        <ul class="nav nav-pills pull-right">
          <li class="active"><a href="#">首页</a></li>
          <li class="active"><a href="/post">新建文章</a></li>
          <li class="active"><a href="/account">会员</a></li>
        </ul>
        <p class="lead"><a href="/">成都婚庆网</a></p>
      </div>
      <hr>
  </div>

<!-- ==== Main Contents ============================================ -->
<div class="container">
	<% blogs.forEach( function(blog){ %>
	<p>
		<div class="post">
			<h2><%= blog.title %></h2>
		</div>
		<span class="underlined"></span>
		<br/>
		<!-- <%=: blog.date | displayDate %> by <%= blog.author %> -->
		<br/>
		<div class="entry">
			<%= blog.body %> <br/>
		</div>
		</br>
		<%= blog.tags %> <br/>


    <% if(blog.comments) { %>
      <span class="underlined"></span>
      <br/>
      <% blog.comments.forEach( function(comment){ %>
        <strong><%= comment.author %> </strong>
        <br/>
        <%= comment.body %>
        <br/>
        <br/>
      <% }); %>
    <% } %>


    <div id="comments">
      <form action="/postComment" method="POST" class="form-horizontal">
        <meta itemprop="postId" content="<%= blog.id %>"/>
        <fieldset>
            <input type="hidden" name="postId" value="<%= blog._id %>" /><br />
            <textarea id="body" name="body" placeholder="欢迎输入评论..."  rows="5" class="span8" ></textarea>
            </br>
            </br>
            <button type="submit" class="btn btn-mini">提交</button>
        </fieldset>
        <br/>
      </form>
    </div>

		<p style="text-align: center;">
			<span class="hex">*</span>
			<span class="hex">*</span>
			<span class="hex">*</span>
		</p>
	</p>
  <% }); %>
  <br/>
  <br/>

</div>

</body>
</html>
